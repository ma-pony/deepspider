# 验证记录

> 模板用途：记录 Python 实现与浏览器原生结果的多样本对比验证。
> 至少完成 3 组输入输出对比，才可进入 handoff 阶段。

---

## 验证目标

| 字段 | 值 |
|------|---|
| 验证参数 | `[填写被验证的加密参数名]` |
| Python 实现文件 | `[填写路径]` |
| 验证方法 | `[Node.js 沙箱 / 浏览器控制台 / HTTP 重放]` |
| 验证时间 | `[填写]` |

---

## 输入输出样本对

> 每组样本需在浏览器中实际触发，记录真实的输入和输出。

### 样本 1

**输入参数：**
```json
{
  "[参数名]": "[填写值]",
  "[时间戳等动态字段]": "[填写值]"
}
```

**浏览器原生输出：**
```
[填写浏览器中实际产生的加密值]
```

**Python 实现输出：**
```
[填写 Python 代码产生的结果]
```

**结果：** `[一致 / 不一致]`

---

### 样本 2

**输入参数：**
```json
{
  "[参数名]": "[填写值，与样本1不同]"
}
```

**浏览器原生输出：**
```
[填写]
```

**Python 实现输出：**
```
[填写]
```

**结果：** `[一致 / 不一致]`

---

### 样本 3

**输入参数：**
```json
{
  "[参数名]": "[填写值，覆盖边界场景]"
}
```

**浏览器原生输出：**
```
[填写]
```

**Python 实现输出：**
```
[填写]
```

**结果：** `[一致 / 不一致]`

---

### 样本 N（可选追加）

<!-- 复制上方样本块继续添加 -->

---

## 中间检查点

> 如果算法包含多个步骤（如：字段拼接 → HMAC → Base64），对每个步骤分别验证。

| 步骤 | 描述 | 浏览器中间值 | Python 中间值 | 是否一致 |
|------|------|------------|--------------|---------|
| Step 1 | `[例如：参数排序拼接]` | `[填写]` | `[填写]` | `[是 / 否]` |
| Step 2 | `[例如：HMAC-SHA256 计算]` | `[填写]` | `[填写]` | `[填写]` |
| Step 3 | `[例如：Base64 编码]` | `[填写]` | `[填写]` | `[填写]` |

---

## 跨环境交叉验证

| 环境 | 工具/方式 | 验证结果 | 备注 |
|------|---------|---------|------|
| 浏览器（Chrome DevTools） | 控制台直接调用 | `[通过 / 失败]` | [填写] |
| Node.js | `deepspider evaluate_script` | `[通过 / 失败]` | [填写] |
| Python | 本地运行实现代码 | `[通过 / 失败]` | [填写] |
| HTTP 重放 | 使用 Python 实现值发起真实请求 | `[通过 / 失败 / 未测试]` | [填写] |

---

## 动态参数处理验证

> 记录时间戳、随机数、nonce 等动态字段的处理方式是否正确。

| 动态参数 | 处理方式 | 验证结论 |
|---------|---------|---------|
| `[参数名，例如 timestamp]` | `[例如：int(time.time() * 1000)]` | `[与浏览器行为一致 / 存在偏差]` |
| `[参数名，例如 nonce]` | `[例如：随机 16 位 hex]` | `[填写]` |

---

## 已知差异与说明

<!-- 记录已知的、可接受的差异，例如随机盐导致每次输出不同但服务端均可验证 -->

1. [填写，例如：nonce 字段每次随机，输出必然不同，但服务端验证逻辑不依赖固定值]
2. [填写]

---

## 验证结论

| 字段 | 值 |
|------|---|
| 整体验证状态 | `[通过 / 部分通过 / 失败]` |
| 通过样本数 | `[N / 总数]` |
| 是否可进入 handoff | `[是 / 否，原因：填写]` |
| 遗留问题 | `[填写，若无填"无"]` |
