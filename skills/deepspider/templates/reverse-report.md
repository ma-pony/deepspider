# 逆向分析报告

> 模板用途：面向交付的最终报告，供调用方（爬虫工程师/团队）使用。
> 语言风格：清晰、简洁、可操作。

---

## 1. 目标概览

| 字段 | 值 |
|------|---|
| 目标站点 | `[填写站点名称及域名]` |
| 目标接口 | `[填写完整 URL 路径]` |
| 请求方法 | `[GET / POST]` |
| 分析目的 | `[填写，例如：还原签名算法，实现自动化数据采集]` |
| 报告生成时间 | `[填写]` |

---

## 2. 复杂度评估

**综合评级：** `L[1/2/3/4]` — `[一句话描述，例如：标准 HMAC-SHA256 签名，L2 级别]`

| 维度 | 评估 | 说明 |
|------|------|------|
| 混淆程度 | `[低 / 中 / 高]` | [填写] |
| 环境依赖 | `[无 / 少量 / 大量]` | [填写] |
| 反调试强度 | `[无 / 弱 / 强]` | [填写] |
| 密钥稳定性 | `[固定 / 周期更换 / 实时下发]` | [填写] |
| 维护预期 | `[稳定 / 需定期检查 / 高风险]` | [填写] |

---

## 3. 加密分析

### 3.1 加密参数汇总

| 参数名 | 位置 | 算法 | 输出格式 | 每次变化 |
|--------|------|------|---------|---------|
| `[参数名]` | `[Header / Body / Query / Cookie]` | `[算法名]` | `[hex32 / base64 / ...]` | `[是 / 否]` |

### 3.2 算法详情

**算法：** `[填写，例如：HMAC-SHA256]`

**输入构造：**
```
[描述签名字段的构造方式，例如：
sorted(params.keys()) + ":" + timestamp + ":" + secret_key]
```

**密钥：**
```
类型：[硬编码 / 接口下发]
值/获取方式：[填写]
```

**输出处理：**
```
[描述输出的后处理步骤，例如：取 hex 后截取前 32 位]
```

---

## 4. 实现方案

### 4.1 技术选型

| 方案 | 可行性 | 推荐度 | 说明 |
|------|--------|--------|------|
| 纯 Python 实现 | `[高 / 中 / 低]` | `[推荐 / 备选 / 不推荐]` | [填写] |
| execjs 调用 JS | `[填写]` | `[填写]` | [填写] |
| Playwright 驱动浏览器 | `[填写]` | `[填写]` | [填写] |

**推荐方案：** `[填写选定方案及原因]`

### 4.2 核心实现

```python
# [填写核心算法的 Python 实现]
# 文件位置：[填写路径]

import hashlib
import hmac
import base64
import time

def build_sign(params: dict, secret_key: str) -> str:
    """
    [填写函数说明]
    
    Args:
        params: 请求参数字典
        secret_key: 签名密钥
    
    Returns:
        签名字符串
    """
    # [填写实现，或标注"见完整实现文件"]
    pass
```

### 4.3 请求示例

```python
import requests

def fetch_data(query: str) -> dict:
    url = "[填写接口 URL]"
    timestamp = int(time.time() * 1000)
    params = {
        "query": query,
        "timestamp": timestamp,
        # [其他参数]
    }
    params["sign"] = build_sign(params, SECRET_KEY)
    
    headers = {
        "User-Agent": "[填写]",
        # [其他必要 Header]
    }
    
    resp = requests.post(url, json=params, headers=headers)
    return resp.json()
```

---

## 5. 验证结果

| 指标 | 结果 |
|------|------|
| 验证样本数 | `[填写]` |
| 全部通过 | `[是 / 否]` |
| 发起真实请求验证 | `[是 / 否]` |
| 服务端响应状态 | `[200 正常 / 其他：填写]` |

**验证结论：** `[一句话总结，例如：Python 实现与浏览器输出完全一致，真实请求返回 200 且数据正常]`

---

## 6. 已知限制

<!-- 诚实列出当前实现的局限性 -->

1. `[填写，例如：密钥硬编码，若网站更新可能失效，建议每周检查一次]`
2. `[填写，例如：IP 频率限制约为每分钟 60 次，超出会触发 403]`
3. `[填写，若无已知限制填"无已知限制"]`

---

## 7. 产出文件

| 文件 | 路径 | 说明 |
|------|------|------|
| 核心算法实现 | `[填写路径]` | [填写] |
| 完整爬虫项目 | `[填写路径，若有]` | [填写] |
| 验证脚本 | `[填写路径]` | [填写] |
| 补环境 Bundle | `[填写路径，若有]` | [填写] |

---

## 8. 使用说明

### 环境要求

```bash
# Python 版本
python >= 3.10

# 依赖安装
pip install [填写依赖包，例如：requests pycryptodome]
```

### 快速开始

```bash
# [填写运行命令]
python [填写入口文件] --url "[填写]" --keyword "[填写]"
```

### 配置说明

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| `SECRET_KEY` | `[填写]` | [填写说明] |
| `REQUEST_INTERVAL` | `[填写，例如：1.0]` | 请求间隔（秒），建议不低于此值 |
| `[其他配置]` | `[填写]` | [填写] |

---

## 9. 建议与风险提示

**短期建议：**
- [填写，例如：建议在请求中随机化 User-Agent，降低特征识别风险]

**长期维护：**
- [填写，例如：该站点签名算法版本号存储在 JS Bundle URL 中，若 URL 变化说明算法已更新]

**风险等级：** `[低 / 中 / 高]`

**风险说明：** `[填写，例如：密钥每 30 天轮换一次，需建立监控机制]`
